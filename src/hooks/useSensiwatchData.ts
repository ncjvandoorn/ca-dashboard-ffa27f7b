// Placeholder hook — will be wired to the sensiwatch-data edge function
// once the API response format is confirmed with real data

export function useSensiwatchTrips() {
  return { data: [], isLoading: false };
}
